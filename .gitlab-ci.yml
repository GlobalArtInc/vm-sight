image: node:latest

stages:
  - build
  - deploy

services:
  - docker:dind

build:
  stage: build
  script:
    - cd ./client && npm run build
    - cd ./api
    - rm -r -f ./api/dist
    - mv ./client/dist ./api
    - docker build -t vm-sight .
