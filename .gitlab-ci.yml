image: node:latest

stages:
  - deploy

services:
  - docker:dind

build:
  stage: deploy
  script:
    - cd ./client && yarn build && cd ..
    - cd ./api
    - rm -r -f dist
    - mv ../client/dist .
    - docker build -t vm-sight .
