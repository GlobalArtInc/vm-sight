{
  "name": "vm-sight",
  "private": true,
  "dependencies": {
    "@kubernetes/client-node": "^0.14.3",
    "dotenv-defaults": "^2.0.2"
  },
  "devDependencies": {
    "cypress": "^8.6.0",
    "lerna": "^3.22.1",
    "sequelize-cli": "^6.4.1"
  },
  "scripts": {
    "preinstall": "npx only-allow pnpm"
  }
}
